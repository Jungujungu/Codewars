def solution(number):
    sum_total = []
    for num in range(number-1):
        if (num+1) % 3 ==0:
            sum_total.append(num+1)
        elif (num+1) % 5 ==0:
            if (num+1 % 3) !=0:
                sum_total.append(num+1)
    return sum(sum_total)